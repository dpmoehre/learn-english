# 英语学习台 — Claude Code / Cursor / Codex / Agent 项目规则

本文件为 **Claude Code**、Cursor、Codex 及其他兼容的 AI Agent 提供 **本仓库** 级约定，请严格遵循。

> **双文件关系**：根目录 [AGENTS.md](AGENTS.md) 不重复正文，只**引用本文件（CLAUDE.md）**为唯一事实来源。规则以本文件为准；两者供不同工具默认读取。**改规则时改本文件即可**，AGENTS.md 保持引用不必同步正文。

约定借鉴自 `p000_sd_sma_scada` / `003_hte_softreg`：施工牌、倒序 `todo.md`、需求先入库、README 只写用途。

---

## 项目定位

- **名称**：英语学习台（对外也叫商务英语速成）。
- **用途**：给刚毕业、英语偏弱的外企新人，用**中文带学**做入门课、外企词典、单词表和练习工具。
- **不是**：带登录的 App、录音评分、自动翻译、网页里的 AI 口语陪练。陪练口语在 **Cursor 对话**里做，不写进页面。
- **技术**：纯静态 HTML / CSS / JS，无构建、无后端、无账号。进度只存在当前浏览器 `localStorage`，不跨设备同步。
- **发布**：GitHub Pages，`main` 分支根目录。
  - 线上：https://dpmoehre.github.io/learn-english/
  - 远程：`https://github.com/dpmoehre/learn-english.git`
  - 本地：`/Users/dp/Documents/文档X/商务英语速成`
- **使用者**：同一人用电脑浏览器、iPhone Safari、iPad Safari。改完跟读或统计后，默认推到 `origin/main`，方便手机刷新。

---

## 架构概览

```
仓库根目录
├── CLAUDE.md / AGENTS.md   # Agent 规则（本文件为正文）
├── README.md               # 用途与用法（不写日期流水账）
├── todo.md                 # 按日期倒序的精炼索引
├── docs/                   # 施工牌：NNN-状态-主题.md
├── _Doc/001_需求整理/      # 用户原话整理成需求记录
├── index.html              # 页面壳、场景、句卡、邮件、测验、朗读引擎
├── beginner.js             # 7 课 + 外企词典 WORDS
├── vocab.js                # 主题单词表
├── experience.js           # 导航、统计、考核、本地历史
├── experience.css          # 统计与导航补样
└── manifest.json
```

不要为这个站点加打包器、框架或后端，除非用户明确要求。

---

## 产品红线

1. **中文带学**：按钮、说明、解析用简体中文；英文只出现在例句、示范和要跟读的句子里。
2. **外企新人**：词和课要能开口用，不堆四六级词表。
3. **无登录**：禁止账号、云同步、外发学习内容做分析。
4. **朗读**：iPad / 电脑默认**系统朗读**；iPhone 默认**网络发音**（任意句子 TTS，不能再用有道词典接口当整句）。系统没声时，页面上可切换「系统朗读 / 网络发音」。
5. **静音拨片**：iPhone 系统朗读常被静音；网络发音走媒体音量。不要只提醒「打开拨片」而不给网络备路。
6. **Agent 对用户说话**：简体中文，短句，少术语。
7. **Clash / 代理配置**不进本仓库。

---

## 需求怎么记

用户每提一条新能力或改法，先写入 [`_Doc/001_需求整理/需求记录.md`](_Doc/001_需求整理/需求记录.md)，再开工。

一条需求至少包含：

- 提出人、提出时间
- 需求描述（尽量保留用户原意）
- 状态：未开始 / 进行中 / 已实现 / 搁置
- 实现内容（做完再补）
- 变更记录（日期 + 做了什么）
- 相关施工牌

不要把需求细节只写在聊天里。相近主题追加在同一条下，不另起炉灶。

---

## 任务事项推进与跟踪

所有功能、修复、文档任务都在 **`docs/`** 维护施工牌。`todo.md` 只保留按日期**倒序**的精炼索引。

### 任务文档命名

1. **相近任务复用优先**：新建前先扫 `docs/` 文件名；已有相近主题则在该文件追加 H1，不另建。
2. **编号递增**：`NNN-状态-主题.md`（三位数字，取现有最大 +1）。
3. **状态与文件名一致**：⌛️ 未启动 / 🚧 进行中 / ✅ 已闭环；状态变化要重命名，编号与主题稳定。
4. **用户指定文件优先**：用户点名某 `*.md` 时，写回该文件，不自动新开编号。

### 任务文档结构

子任务必须是 H1：

- `# ✅ 已完成：…`
- `# 🚧 进行中：…`
- `# ⌛️ 未完成：…`

详细信息（目标、范围、验收、证据、风险）写在对应 H1 下。部分完成必须拆分，不要把父项标 ✅。

### 推进流程

1. **执行前**：查 `todo.md` 与 `docs/`；开始实施则保证文件名为 `NNN-🚧-主题.md`。需求先落 `需求记录.md`。
2. **执行中**：随进展改 H1 与证据；独立大任务再开下一编号。
3. **执行后**：闭环标 ✅ 并重命名文件；在 `todo.md` **顶部**追加日期分段，引用施工牌，不复制全文；**README.md** 只改用途/用法。

---

## Git 与版本控制

- 已配置 `origin`：完成一条施工牌任务后 `git add` + `commit` + `push`（更新 GitHub Pages）。
- 用户说暂不提交 / 暂不推送 / 仅本地保存：尊重。
- **不要提交**：密钥、`.env`、本机 Clash 配置、无关大文件。
- 提交说明写「为什么」，前缀建议：`fix(speak):` / `feat(stats):` / `docs:`。
- 除用户要求外，不改 git config，不用 `--no-verify`，不 force push。

---

## 验证

改跟读、导航、统计、答题时：

- 能开浏览器就按真用户点一遍（听一句、答情景题、看统计）。
- 手机相关改动：推 Pages 后写明刷新方式；本机无法代替 iPhone 听声时，在施工牌写「待用户真机确认」。
- 无浏览器时：至少做 JS 语法检查，并在回复里写清未验证项。

---

## 文件命名约定

- 源码：`index.html`、`*.js`、`*.css` 保持现名，ASCII。
- 普通 Markdown：`YYYY-MM-DD-` + 主题。
- 施工牌：`docs/NNN-状态-主题.md`，不用日期前缀。
- 需求记录：固定文件，按日期追加章节，不按条新建文件。

---

## 约定摘要

| 环节 | 动作 |
|------|------|
| 新需求 | 先写入 `_Doc/001_需求整理/需求记录.md` |
| 任务文档 | `docs/NNN-状态-主题.md`；先复用再递增 |
| 子任务 | H1：✅ / 🚧 / ⌛️ |
| 收尾-记录 | 细节在施工牌；精炼索引倒序写入 `todo.md` |
| 收尾-README | 只维护用途与用法 |
| Git | 有 remote 则任务完成即 commit+push；用户反对则尊重 |
| 规则维护 | 只改本文件；AGENTS.md 只引用 |

---

> 本仓库以本文件为 Agent 共同约定的**唯一正文**，[AGENTS.md](AGENTS.md) 引用之。改规则改本文件即可。
